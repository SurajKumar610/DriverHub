# DriverHub
The Driver Management Web Application is a full-stack system built with React, Node.js, Express, and PostgreSQL. It supports secure authentication and complete CRUD operations for managing driver records. The app features search, filtering, and a responsive UI, and is fully Dockerized for scalable, production-ready deployment.
